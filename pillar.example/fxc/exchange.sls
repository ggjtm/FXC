fxc:
  exchange:
    # Pre-built distribution tarball (./gradlew :FxcExchange:distTar) staged somewhere fetchable —
    # docs/PLAN.md Phase 8 item 11, the publish step this depends on doesn't exist yet.
    artifact_url: CHANGEME
    artifact_sha256: CHANGEME
